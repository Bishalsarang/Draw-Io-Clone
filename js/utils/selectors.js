/**
 * selectors.js
 * Contains alias for common selectors
 */

/**
 * Alias for document.querySelector(selector)
 *
 * @param {string} selector css selector
 */
function $(selector) {
	return document.querySelector(selector);
}
